# OrgMind PM Extension Dependencies
# 
# These are additional dependencies specific to the PM extension.
# Core OrgMind dependencies are assumed to be already installed.

# Configuration parsing
pyyaml>=6.0.1

# Data processing
pandas>=2.1.0
numpy>=1.24.0

# Graph algorithms (for impact analysis, critical path)
networkx>=3.2.0

# Optimization (for sprint planning, resource allocation)
pulp>=2.7.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Linting (development)
yamllint>=1.33.0
